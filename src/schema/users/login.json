{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://example.com/root.json",
    "type": "object",
    "title": "The User login Schema",
    "required": [
        "email",
        "password"
    ],
    "properties": {
        "email": {
            "$id": "#/properties/username",
            "type": "string",
            "title": "The Username Schema",
            "format": "email"
        },
        "password": {
            "$id": "#/properties/password",
            "type": "string",
            "title": "The Password Schema",
            "pattern": "^(.*)$"
        }
    }
}